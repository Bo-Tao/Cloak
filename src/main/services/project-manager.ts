/**
 * Project management is implemented directly in src/main/ipc/handlers.ts
 * via the PROJECT_LIST, PROJECT_ADD, and PROJECT_CLAUDE_MD IPC handlers.
 *
 * Project data is persisted in electron-store under the 'projects' key.
 */

export {}
